<%@ page language="java" contentType="text/html; charset=ISO-8859-1"
    pageEncoding="ISO-8859-1"%>
    
<%@ taglib prefix = "c" uri = "http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="form" uri="http://www.springframework.org/tags/form" %>

<!DOCTYPE html>
<html>
<head>
<meta charset="ISO-8859-1">
<title>Table Comparison</title>
</head>
<body>

	<form action="${pageContext.request.contextPath}/addToTable" method="post" >
		<select name="Add_Fighter">
			<option value="">Select Fighter</option>
		    <c:forEach items="${allFighters}" var="fighter">
		        <option value="${fighter.id}">${fighter.name}</option>
		    </c:forEach>
	    </select>
	    <br>
	    <input type="submit" value="Add to Table"/>
	</form>
	
	<br>
	
	<fieldset>
		<legend>Table comparison</legend>
		<table>
			<tr>
				<th>Name</th>				
				<th>SLpM</th>
				<th>SApM</th>
				<th>TDp15</th>
				<th>SubAtp15</th>
				<th>ko%</th>
				<th>fin%</th>
				<th></th>
			</tr>
			<c:forEach items="${tableList}" var="fighter">
				<tr>
					<td>${fighter.name}</td>
					<td>${fighter.sigLpm}</td>
					<td>${fighter.sigApm}</td>
					<td>${fighter.tdpF}</td>
					<td>${fighter.subPF}</td>
					<td>${fighter.koPF}</td>
					<td>${fighter.finRate}</td>
					<td>
						<a href="${pageContext.request.contextPath}/removeFromTable/${fighter.id}">Remove from table</a>	
					</td>
				</tr>
			</c:forEach>
		</table>
	</fieldset>
	
	<div> <a href="${pageContext.request.contextPath}/userPage">Back to user page</a> </div>
</body>
</html>